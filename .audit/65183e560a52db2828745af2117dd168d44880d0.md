---
sha: 65183e560a52db2828745af2117dd168d44880d0
subject: "Bump staged-review 1.18.0 → 1.18.1: fix hooks.json schema (missing top-level hooks: wrapper)"
author: E.FU
date: 2026-05-09
verdict: clean — fast-path
loc: 24
files_changed: 2
lib_touched: false
codex_dispatched: false
---

## Fast-Path Classification

- **LOC:** 24 (14 additions, 12 deletions)
- **Files:** `plugins/staged-review/.claude-plugin/plugin.json`, `plugins/staged-review/hooks/hooks.json`
- **Production-code paths (`lib/`):** none — plugin config + hook schema only
- **Eligible:** ≤100 LOC AND no `lib/` → fast-path verdict, no full 5+1 audit, no Codex dispatch

## Change Summary

Wraps `SessionStart` hook entry under the required top-level `"hooks": { ... }` record in `plugins/staged-review/hooks/hooks.json` and bumps plugin patch version `1.18.0 → 1.18.1`.

The schema fix matches the shape used by every other plugin in the repo (`elixir`, `cloud-delegation`). Pre-existing latent bug from v1.17 when the SessionStart hook was first added.

## Verdict

`clean — fast-path`. Mechanical schema correction, version bump, no behavioral risk. Copilot reviewed the PR (state COMMENTED) and raised no findings.
